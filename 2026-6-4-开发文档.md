# 2026-6-4 至 2026-6-5 开发文档

本轮开发围绕 `FJ-QED/FJpy-win` 的三个示例 notebook 展开，目标不是单纯把互相关、频散成像和反演流程拼接在一起，而是把 `example_CC.ipynb`、`example_noise.ipynb` 和 `example_EQ.ipynb` 都整理成可以顺序执行、可以直接展示、并且能够在同一份 notebook 中完成“观测频散构造、频散拾取、地下速度结构反演、理论频散回代对比”的完整技术样例。用户明确要求所有 `.ipynb` 必须只用 Python 以 `encoding='utf-8'` 读写，不能通过终端重定向直接修改 notebook；同时要求补足中文正文、公式、参数说明和中文自检，最终还要更新开发文档并同步到 GitHub。后续所有改动都围绕这些约束执行。

最初阶段的工作重点是把原有的 Win 版 notebook 与 `QEDispInv-win` 的反演能力真正接起来。为此，在 `FJpy-win/examples` 的三个 notebook 末尾统一追加了新的反演区块，并通过 [append_inversion_to_fj_notebooks.py](E:/codes/DFSpy/tools/append_inversion_to_fj_notebooks.py) 以 UTF-8 JSON 的方式写回 notebook。这个脚本的职责不是简单插入几个 code cell，而是统一生成标题、方法说明、参数解释、辅助函数、模型设置、反演调用、绘图展示和中文自检单元。这样做的直接好处是，三个 notebook 的结构保持一致，后续如果要统一改频散拾取逻辑、补正文说明或修复反演参数，只需要维护一个脚本即可，不需要手工分别编辑三个 `.ipynb` 文件。

在频散拾取方法上，最早的演示版本使用的是相对简化的单脊线动态规划思路。这种方法对展示“怎样从一张频散图上找一条主导曲线”是够用的，但对于用户要求的“包含基阶和高阶模式，并且用于正规反演”明显不够。因此，本轮进一步参考了 `DisperNet` 开源仓库中已经公开的后处理思想，把 notebook 里的频散拾取流程改造成“候选峰值提取、模式分离、质量控制、约束跟踪、插值平滑”的完整链条。由于 `DisperNet` 公共仓库里的 `pick(...)` 接口依赖远程模型服务，而该模型和在线接口并不在当前仓库中，所以这里没有照搬其在线推理部分，而是保留了可以本地复现的核心思想：先在频散图的每个频率列上找候选峰值，再用模式聚类和连续性约束把这些候选点组织成基阶和高阶频散曲线，最后再把曲线送入反演模块。这一方案既满足了“采用 DisperNet 方法”的要求，也保证了当前工程能够在本地独立运行。

为了支撑上述流程，脚本里新增了一组专门面向 notebook 的辅助函数。其中包括候选点提取、DisperNet 风格模式分离、模式质量控制、模式带内连续跟踪、频率重采样和平滑、参考模型构造、反演调用、理论频散正演和统一绘图函数。这里最关键的改动有两个。第一个改动是补上了模式重编号逻辑 `reindex_modes(...)`。在之前的版本里，经过质量控制后只剩下一支模式时，这支曲线仍然可能保留旧编号 `1` 或 `2`，而不是新的 `0`。这会直接破坏观测模式与理论模式的对应关系。第二个改动是新增了 `filter_valid_forward_pairs(...)`，在统计反演拟合质量前，先把理论正演中无法计算的点过滤掉，只对同时拥有观测值和理论值的点计算失配。配合新的有限值统计逻辑，之前 `example_CC.ipynb` 中 `NaN` 失配的故障被彻底消除。

在 notebook 正文层面，本轮补充的内容不是装饰性文字，而是围绕解释性和可复现实验设计来写的。三个 notebook 的追加区块都加入了多级标题，对方法原理、拾取流程、反演参数和结果解释做了分段说明。公式部分统一使用 `$...$` 和 `$$...$$` 的写法，以保证 Jupyter 中的数学符号显示正常。正文中专门补入了 `Median Vs`、`P10 Vs`、`P90 Vs` 的定义：其中 `Median Vs` 表示多组有效反演模型在每一深度处的中位数剪切波速度，用来代表稳健的中心趋势；`P10 Vs` 和 `P90 Vs` 分别表示速度分布的 10% 与 90% 分位数，用来描述反演结果在不同深度处的不确定性范围。这一解释直接写入 notebook，而不是只留在脚本或文档里，目的是让最终使用 notebook 的读者不需要跳出当前文件就能理解图上的阴影带和中值曲线各自意味着什么。

反演参数说明也是本轮正文补充的重要部分。此前用户明确指出，演示级配置不能满足正规反演流程的要求，因此本轮在 notebook 正文中补充了核心参数的物理意义和常用调节方向。例如，`weight` 用于指定不同模式失配项的相对权重，通常基阶应大于高阶，因为基阶连续性最好、物理稳定性也最高；`sigma` 表示各模式的观测不确定度，会直接影响扰动重采样时允许的波动范围；`lambda_` 控制模型平滑或正则化强度，过小容易过拟合，过大则会把速度结构压得过于平滑；`vs_width` 决定初始剪切波速度模型允许的扰动幅度；`num_init` 和 `num_noise` 分别控制多初值与噪声重采样的数量；`rmin`、`rmax` 和 `maxiter` 则分别影响扰动比例范围和单次优化的充分程度。正文中还强调了一个原则：优先通过控制频带、模式质量和合理先验来提高反演稳定性，而不是盲目增加模式数量或无节制放大扰动。

`example_CC.ipynb` 的修复重点在于让交叉互相关案例重新获得可用的反演结果。这个案例的观测频散本身比较单纯，因此最终保留下来的主要是一支模式。问题不在于“只有一支模式”，而在于之前这支模式被错误地保留为旧编号，导致后续正演对不上。修复后，`example_CC.ipynb` 重新执行的摘要为：`picked_points=223`，`mode_counts={0: 223}`，`fitness_min=1.8543648232764731`，`fitness_mean=1.8580953749383298`，`mean_abs_phase_velocity_misfit_km_s=0.37338823177234565`，`median_abs_phase_velocity_misfit_km_s=0.283869912269509`，`num_valid_models=16`，`depth_max_km=25.0`。这说明 `CC` 路线虽然仍是单模主导，但理论频散和观测频散已经能够稳定比较，之前的 `NaN` 失配问题已经消失，整条流程回到了“可解释”的状态。

`example_noise.ipynb` 的目标是展示环境噪声频散图中基阶与高阶模式的联合约束，因此这一案例更适合保留多模态信息。不过，前一版参数虽然保留了更多点数和三支模式，但代价是运行时间显著上升，而且最弱的一支高阶模式稳定性不足，会把不连续的尾部点也带进反演。为了解决这个问题，本轮把输入矩阵规模调为 `24 x 192`，把速度采样压到 220 个，同时对用于反演的频率采样做隔点抽样，以减少不必要的高密度重复点。在当前的数据质量约束下，最终稳定保留下来的主要是基阶和一阶高模，摘要为：`picked_points=62`，`mode_counts={0: 24, 1: 38}`，`fitness_min=1.0438471259430728`，`fitness_mean=1.084241910365393`，`mean_abs_phase_velocity_misfit_km_s=0.3361221041031636`，`median_abs_phase_velocity_misfit_km_s=0.31455426641648465`，`num_valid_models=28`，`depth_max_km=40.0`。从结果上看，这并不是“模式变少了”，而是质量控制开始主动清理不稳定高阶点，使最终保留下来的模式更适合做反演解释。

`example_EQ.ipynb` 是本轮最需要重点处理的案例，因为用户明确指出它上一版的理论频散曲线与观测频散差距过大，不能只停留在演示级。为此，本轮对 `EQ` 的候选频带、模式连续性约束、先验权重和模式使用频段做了系统性调整。候选点搜索频带上限收紧到 `0.16 Hz`，一阶高模进一步限制在 `0.14 Hz` 以内，以压掉高频段最不稳定的异常点。连续性权重和先验权重分别提高到 `95.0` 和 `45.0`，使模式跟踪更偏向连续、物理可解释的曲线。反演阶段提高基阶权重、适当保留高阶权重，并把迭代上限提高到 50 步，配合更紧的 `sigma` 设置，让求解器优先拟合主导分支，再由高阶模式提供补充约束。重新执行后的摘要为：`picked_points=70`，`mode_counts={0: 45, 1: 25}`，`fitness_min=1.0333831374631857`，`fitness_mean=1.126859255199298`，`mean_abs_phase_velocity_misfit_km_s=0.3103236848115867`，`median_abs_phase_velocity_misfit_km_s=0.09294866922239975`，`num_valid_models=27`，`depth_max_km=70.0`。这个结果说明新的 `EQ` 已经不再是“高阶模式拖坏整体”的状态，而是主导模式拟合明显收敛，高阶模式作为辅助约束保留在合理范围内。

在 notebook 执行方面，本轮没有使用 `nbconvert` 或终端重定向去直接重写 `.ipynb`，而是新增了 [execute_notebooks_with_jupyter_client.py](E:/codes/DFSpy/tools/execute_notebooks_with_jupyter_client.py) 作为统一执行器。这个脚本基于 `jupyter_client` 逐个执行 code cell，把输出回填到 notebook 的 JSON 结构中，再用 `encoding='utf-8'` 写回磁盘。通过这种方式，既满足了用户“不用终端重定向改 notebook”的要求，也确保了每个 notebook 的执行顺序、输出内容和最终存盘状态彼此一致。本轮已经用该脚本依次执行了 `example_CC.ipynb`、`example_noise.ipynb` 和 `example_EQ.ipynb`，并检查了最后的反演摘要、图形单元和中文自检单元，确认三个 notebook 的追加区块都已经正常落盘。

中文与编码自检也是这次交付里明确做过的一步。除了在脚本中保留“写入追加区块前检查新增文本中是否出现问号占位符”的逻辑之外，还对三个 notebook 的落盘内容做了额外检查，确认最后追加区域中没有出现新的乱码占位符。每个 notebook 末尾都保留了中文自检单元，执行后会输出“追加反演区块已按 UTF-8 写入，且未出现乱码占位符”的中文提示。由于 Matplotlib 默认字体在某些环境下仍会对中文标题给出 glyph 缺失警告，因此新增的三联图标题使用了英文短标题，例如 `Velocity Model`、`Observed Dispersion Image` 和 `Observed vs Synthetic Dispersion`，这样既不影响 notebook 正文的中文解释，也避免了保存输出时出现与编码无关的字体警告。

从本轮开发结果看，三个 notebook 现在已经形成了统一而完整的结构。每个 notebook 在最后都能稳定给出一张速度结构图、一张观测频散图，以及一张观测频散与反演后理论频散的对比图。更重要的是，这些图不再只是“为了出图而出图”，而是对应着经过模式管理、参数约束和反演回代验证后的结果。`CC` 目前适合作为单模主导情况下的最小完整流程示例，`noise` 适合作为多模态环境噪声案例，`EQ` 则展示了在地震激发条件下如何通过收紧模式和正规化参数把拟合拉回合理范围。当前版本已经可以作为后续进一步扩展或整理教学材料的基础版本。

最终交付物包括更新后的三个 notebook、维护 notebook 追加区块的脚本、用于顺序执行 notebook 的执行脚本，以及本开发文档本身。所有涉及 `.ipynb` 的修改都通过 Python UTF-8 读写完成，三个 notebook 也都经过了重新执行和结果回读检查。后续如果还需要继续提高某一案例的拟合精度，应优先从模式频带选择、候选峰值质量控制和初始速度模型合理性三个方向入手，而不是直接无差别增加模式数或放大模型扰动。
